export const HomeContainerCard = ({title, description}) => {
    return (
        <div className="home-container-card">
            <h2>{title}</h2>
            <p>{description}</p>
        </div>
    )
}